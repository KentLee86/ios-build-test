name: WarpBuild Linux Smoke

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke:
    runs-on: warp-ubuntu-latest-x64-2x
    timeout-minutes: 5
    steps:
      - name: Prove WarpBuild runner allocation
        run: |
          uname -a
          echo "WarpBuild Linux runner is working"
